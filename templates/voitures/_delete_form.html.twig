<form method="post" action="{{ path('app_voitures_delete', {'id': voiture.id}) }}" onsubmit="return confirm('Est vous sûre de vouloir supprimer ce véhicule ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.id) }}">
    <button class="btn">Supprimer</button>
</form>
