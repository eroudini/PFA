{% extends 'base.html.twig' %}

{% block title %}{{product.nomproduit}}{% endblock %}

{% block body %}

<div class="container cardcars">
    <div class="card enfcard">
        <div class="container-fluid">
            <div class="wrapper row">
                <div class="preview col-md-4">                  
                    <div class="preview-pic tab-content">
                      <div class="tab-pane active"><img src="{{ product.images }}" /></div>
                    </div>
                </div>
                <div class="details col-md-6">
                    <h3 class="product-title">{{ product.nomproduit }}</h3>
                    <h4 class="price">Prix: <span>{{ product.prix }}</span></h4>
                    <p class="product-description">{{ product.description }}</p>
                    <div class="action">             
                        {# <button class="nav-link mx-2 text-uppercase" href="{{ path('app_commandes_new') }}" type="button">Acheter</button> #}

                        <a class="nav-link mx-2 text-uppercase btn" href="{{ path('app_commandes_new') }}">Acheter</a>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


    {# PRODUITS ADDITIONNELS
	
	app_commandes_new #}

{% endblock %}
